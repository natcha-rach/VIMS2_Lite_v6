# VIMS2 Lite V10.1 — Next Steps

1. Do not run `schema.sql` on the existing production database.
2. The existing `sql/migration_v10_1_realtime.sql` has already been applied in the current Supabase project.
3. Deploy the frontend files only after local/browser smoke tests.
4. Test in this order:
   - Create Lot
   - Create Group
   - Add 1 Item + 1 image
   - Add 1 Item + 2 images
   - Edit Item
   - Sell Item
   - Check sale on another device
   - Bulk 5
   - Bulk 20
   - Bulk 200
5. `item_images` is the source of truth for product images. Each Item supports `sort_order` 1 and 2.

## Next test order

1. Open Items on desktop and create a test Lot Group.
2. Photo Queue: test 5 items / 5 images with `1 image / item`.
3. Test 5 items / 10 images with `2 images / item`.
4. Test mixed 1/2 images with `manual` mode.
5. Test 20 rows before attempting the full 200-row batch.
6. Open another device and verify Items/Images appear after Realtime refresh.
7. Only after those tests, run the real 200-item batch.

### Important
The browser cannot persist `File` objects in localStorage. A restored Bulk Draft keeps text/price fields but requires the image files to be selected again.
